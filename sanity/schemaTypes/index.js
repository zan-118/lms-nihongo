import { materiType } from "./materi";
import { questionType } from "./question";
import { quizPackageType } from "./package";

export const schema = {
  types: [materiType, questionType, quizPackageType],
};
